{% extends 'base.html.twig' %} {% block title %}Facility{% endblock %} {% block
body %}
<h1>Facility</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ facility.id }}</td>
    </tr>
    <tr>
      <th>Category</th>
      <td>{{ facility.category }}</td>
    </tr>
    <tr>
      <th>Name</th>
      <td>{{ facility.name }}</td>
    </tr>
    <tr>
      <th>Longitude</th>
      <td>{{ facility.longitude }}</td>
    </tr>
    <tr>
      <th>Latitude</th>
      <td>{{ facility.latitude }}</td>
    </tr>
    <tr>
      <th>Img</th>
      <td>{{ facility.img }}</td>
    </tr>
    <tr>
      <th>Description</th>
      <td>{{ facility.description }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('app_facility_index') }}">back to list</a>

<a href="{{ path('app_facility_edit', { id: facility.id }) }}">edit</a>

{% endblock %}
