{% extends 'base.html.twig' %} {% block title %}Edit Scene{% endblock %} {%
block body %}
<h1>Edit Scene</h1>

{{ include("scene/_form.html.twig", { button_label: "Update" }) }}

<a href="{{ path('app_administration', { festivalId: festival.id }) }}"
  >back to list</a
>

{% endblock %}
