{% extends 'base.html.twig' %}

{% block title %}Performer{% endblock %}

{% block body %}
    <h1>Performer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ performer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ performer.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ performer.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ performer.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_performer_index') }}">back to list</a>

    <a href="{{ path('app_performer_edit', {'id': performer.id}) }}">edit</a>

    {{ include('performer/_delete_form.html.twig') }}
{% endblock %}
