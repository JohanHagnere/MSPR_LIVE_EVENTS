{% extends 'base.html.twig' %} {% block title %}Edit Concert{% endblock %} {%
block body %}
<h1>Edit Concert</h1>

{{ include("concert/_form.html.twig", { button_label: "Update" }) }}

<a href="{{ path('app_administration', { festivalId: festival.id }) }}"
  >back to list</a
>

{% endblock %}
